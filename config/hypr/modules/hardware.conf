# ╭─────────────────────────────────────────────────────────────────────────────╮
# │                    LENOVO IDEAPAD FLEX 5 HARDWARE CONFIG                     │
# ╰─────────────────────────────────────────────────────────────────────────────╯

# Input Configuration (IdeaPad Flex 5 optimiert)
input {
    kb_layout = at,de
    kb_variant = nodeadkeys,nodeadkeys
    kb_options = grp:alt_shift_toggle,compose:menu
    
    repeat_rate = 30
    repeat_delay = 300
    
    follow_mouse = 1
    mouse_refocus = true
    float_switch_override_focus = 2
    
    touchpad {
        natural_scroll = true
        tap-to-click = true
        drag_lock = true
        disable_while_typing = true
        scroll_factor = 0.3
        middle_button_emulation = true
        clickfinger_behavior = true
        tap-and-drag = true
    }
    
    sensitivity = 0.8
    accel_profile = adaptive
}

# Touch Gestures für IdeaPad Flex 5 (Convertible)
gestures {
    workspace_swipe = true
    workspace_swipe_fingers = 3
    workspace_swipe_distance = 300
    workspace_swipe_invert = true
    workspace_swipe_min_speed_to_force = 30
    workspace_swipe_cancel_ratio = 0.5
    workspace_swipe_create_new = true
    workspace_swipe_direction_lock = true
    workspace_swipe_forever = false
}
